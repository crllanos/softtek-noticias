import { DateformatcustomPipe } from './dateformatcustom.pipe';

describe('DateformatcustomPipe', () => {
  it('create an instance', () => {
    const pipe = new DateformatcustomPipe();
    expect(pipe).toBeTruthy();
  });
});
